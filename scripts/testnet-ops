#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$repo_root/daemon"
exec env GOTOOLCHAIN=auto go run ./cmd/testnet-ops --deployment ../deployments/arc-testnet.json "$@"
