[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.26"
optimizer = true
optimizer_runs = 200
# Arc testnet — fill after verifying docs.arc.io/arc/references/connect-to-arc
# [rpc_endpoints]
# arc_testnet = "${ARC_TESTNET_RPC}"
