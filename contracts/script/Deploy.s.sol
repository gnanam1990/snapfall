// SPDX-License-Identifier: MIT
pragma solidity ^0.8.26;
// TODO(A): forge-std Script; deploy order: AuditAnchor → JobVault(usdc) → FloatPool(usdc) → wire addresses.
// Commit deployed addresses to docs/addresses.md and README testnet notes. Verify source on Arc explorer if supported.
